#!/bin/sh
# SponsorPulse pre-commit hook
# Runs Biome check + auto-fix on all staged TypeScript/JS files.

echo "Running typecheck"
npx run check

echo "Running linter"
npx run lint

echo "Running format"
npx run format

git add .

echo "Biome check passed. Proceeding with commit."
