{
  "name": "www",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "vite",
    "preview": "vite preview",
    "lint": "bunx biome check --fix .",
    "format": "bunx biome format --write ."
  }
}
