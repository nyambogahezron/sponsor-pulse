{
  "name": "sponsor-pulse-extension",
  "private": true,
  "version": "0.1.1-beta",
  "type": "module",
  "scripts": {
    "build": "tsc && vite build",
    "preview": "bunx serve dist",
    "icons": "node scripts/generate-icons.mjs",
    "check": "tsc -noEmit",
    "lint": "bunx biome check --fix .",
    "format": "bunx biome format --write ."
  },
  "dependencies": {
    "@sponsor-pulse/shared": "workspace:*"
  },
  "devDependencies": {
    "@napi-rs/canvas": "^1.0.0",
    "@types/chrome": "^0.1.42",
    "vite": "^8.0.12",
    "typescript": "~6.0.2"
  }
}
